pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'gulp build'
            }
        }
        stage('Test') {
            steps {
                sh 'gulp test'
            }
        }
        stage('Deploy') {
            steps {
                sh 'gulp deploy'
            }
        }
    }
}
